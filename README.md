# Bugay.Initialize.Docker

The purpose of this project is to be a Windows Service that runs on startup. It will launch WSL
in the background so that the Host OS (Windows) can communicate with the Docker engine running
in WSL.
